const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const posts = await prisma.post.findMany({ include: { category: true, author: true } }); console.log('Posts:', posts.map(p => ({ id: p.id, category: p.category ? 'exists' : 'MISSING', author: p.author ? 'exists' : 'MISSING' }))); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
